# T03 제출문 초안 — 짤·카드 스튜디오

## 0. 구조상 자동으로 만족되는 것들

- **T03-C28 (위치정보 메타데이터 0건)**: 다운로드 버튼은 원본 파일을 그대로 저장하지 않고, `canvas.toBlob()`으로 픽셀을 다시 그려서 새 PNG를 만듭니다. 캔버스로 다시 그린 이미지에는 원본 사진의 EXIF/GPS 메타데이터가 절대 남지 않습니다(브라우저 캔버스는 메타데이터를 아예 다루지 않음). 따로 확인할 필요 없음.
- 미리보기와 다운로드 파일이 항상 일치(T03-C11~13): 화면에 보이는 캔버스를 그대로 PNG로 내보내는 방식이라 애초에 어긋날 수 없는 구조입니다.

## 1. 배포 방법 (GitHub Pages)

새 저장소 `t03-card-studio`를 만듭니다.

```powershell
cd "G:\내 드라이브\SKT_ALEPH\T03"
git init
git add index.html "제출문.md"
git commit -m "T03: 짤·카드 스튜디오"
git branch -M main
git remote add origin https://github.com/yubin05/t03-card-studio.git
```
push는 **사용자님 PowerShell에서 직접**:
```powershell
git push -u origin main
```
push 후 저장소 Settings → Pages → Branch `main` / `/(root)` → Save.
`https://yubin05.github.io/t03-card-studio/` 에서 열립니다. **이 주소가 결과물 URL.**
소스 저장소 URL: `https://github.com/yubin05/t03-card-studio/commit/[커밋해시]` (`git rev-parse HEAD`로 확인)

## 2. 카드1 — 편집과 미리보기

- [ ] 시크릿 창에서 로그인 없이 열리는지
- [ ] 첫 화면에 이미지 도구(파일 선택)와 문구 도구(텍스트/위치/크기/색)가 보이는지
- [ ] PNG 이미지 업로드 → 정상 표시
- [ ] JPEG 이미지 업로드 → 정상 표시
- [ ] 문구 위치 슬라이더 움직이면 미리보기에 즉시 반영되는지
- [ ] 글자 크기 슬라이더 움직이면 즉시 반영되는지
- [ ] 글자 색 바꾸면 즉시 반영되는지
- [ ] 이미지가 있는 상태에서 지원하지 않는 파일(예: .gif, .txt를 이미지처럼 선택) 업로드 시도 → 거부 메시지 뜨고 기존 이미지·문구가 그대로 남아있는지

## 3. 카드2 — 화면비 3개 대조

각 비율에서: 모서리·줄바꿈이 잘 보이는 테스트 문구(예: "가장자리 확인용 문장입니다\n둘째 줄도 있습니다")를 넣고, 다운로드한 PNG를 열어 미리보기와 나란히 비교합니다.

| 화면비 | 미리보기=파일 일치 |
|---|---|
| 1:1 | |
| 4:5 | |
| 9:16 | |

## 4. 카드3 — 극단 입력 12건 검사표

| # | 입력 | 예상 결과 | 실제 결과 |
|---|---|---|---|
| 1 | 매우 긴 한글 문장(200자 이상, 공백 있음) | 여러 줄로 자동 줄바꿈, 잘리지 않음 | |
| 2 | 매우 긴 영어 문장(200자 이상, 공백 있음) | 여러 줄로 자동 줄바꿈 | |
| 3 | 한글+영어 혼합 문장 | 정상 표시 | |
| 4 | 엔터로 여러 줄 나눈 문구 | 입력한 줄바꿈 그대로 유지 | |
| 5 | 이모지 여러 개 연속 (😀😃😄😁😆😅🤣😂🙂🙃) | 깨지지 않고 표시 | |
| 6 | 공백 없는 매우 긴 단어(알파벳 100자 연속) | 글자 단위로 줄바꿈되어 화면 밖으로 안 나감 | |
| 7 | 빈 문구(텍스트 칸 비움) | 에러 없이 이미지만 보임 | |
| 8 | 세로로 긴 이미지 업로드 | 잘리더라도 중앙 기준으로 꽉 채워짐, 에러 없음 | |
| 9 | 가로로 긴 이미지 업로드 | 위와 동일 | |
| 10 | 투명 배경 PNG 업로드 | 투명한 부분에 배경색이 보임 | |
| 11 | 지원하지 않는 파일 형식(.gif 등) 업로드 | 거부 메시지, 기존 내용 유지 | |
| 12 | 확장자만 png인 손상된 파일 업로드 | "이미지를 열 수 없습니다" 메시지, 기존 내용 유지 | |

실제로 FAIL이 나온 항목이 있으면 여기에 수정 전/후를 적으세요:
```
결함: [무엇이 잘못됐는지]
수정 전: [증상]
수정 후: [고친 뒤 증상]
```

## 5. 카드4 — 템플릿 CRUD (실제로 해보기)

1. 서로 다른 이름으로 템플릿 **3개** 저장 (예: "생일카드", "공지카드", "테스트")
2. 하나를 "불러오기" → 문구나 색을 바꾸고 → "현재 내용으로 수정" 클릭
3. 다른 하나를 "삭제"
4. **F5로 새로고침**
5. 남은 템플릿 2개의 이름·내용이 올바르게 유지되는지 확인 (수정한 게 수정된 대로, 삭제한 게 진짜 없어졌는지)

| 확인 | 결과 |
|---|---|
| 3개 생성됨 | |
| 불러오기 정상 | |
| 수정 후 새로고침해도 유지 | |
| 삭제 후 새로고침해도 안 돌아옴 | |

## 6. 카드5 — JSON 백업 및 완성 이미지 3개

**JSON 테스트**
1. "전체 템플릿 내보내기" 클릭 → 파일 저장 (정상 JSON 원본, 보관해두기)
2. 그 파일을 메모장으로 열어 괄호 하나를 지우고 저장 → "가져오기" 시도 → 거부되고 기존 템플릿 유지되는지 확인
3. 원본 파일을 복사해서 템플릿 항목 하나의 `"name"` 줄을 통째로 지우고 저장 → "가져오기" 시도 → 거부되고 기존 템플릿 유지되는지 확인
4. 손대지 않은 원본 파일로 다시 "가져오기" → 정상적으로 템플릿이 복원되는지 확인

| 테스트 | 결과 |
|---|---|
| 정상 JSON 가져오기 → 복원됨 | |
| 문법 깨진 JSON → 거부, 기존 유지 | |
| 필수 항목 빠진 JSON → 거부, 기존 유지 | |

**완성 이미지 3개** (서로 다른 문구 또는 화면비로 3장 만들어서 다운로드)

이미지 저작권 문제를 피하려면 **이미지 업로드 없이 배경색만 사용**해서 3장을 만드는 걸 추천합니다 — 그러면 외부 이미지 출처를 밝힐 필요 없이 전부 "본인 제작"이 됩니다. 본인이 찍은 사진을 쓰셔도 됩니다(그것도 본인 제작). 남이 찍은 사진/저작권 있는 이미지는 쓰지 마세요.

| 파일명 | 화면비 | 문구 요약 | 제작 구분 |
|---|---|---|---|
| [이미지1] | | | 본인 제작 |
| [이미지2] | | | 본인 제작 |
| [이미지3] | | | 본인 제작 |

## 7. 짧은 확인 방법 (4줄)

1. **어디로 가나요**: 결과물 URL을 시크릿 창에서 엽니다.
2. **3단계 이내 무엇을 하나요**: ① 이미지 불러오기(또는 배경색만 사용) ② 문구 입력하고 위치·크기·색 조절 ③ "PNG로 다운로드" 클릭
3. **무엇이 보이면 통과인가요**: 미리보기가 즉시 바뀌고, 다운로드한 PNG가 미리보기와 정확히 같으며, 잘못된 파일을 넣으면 거부 메시지가 뜨고 기존 작업은 그대로 남음
4. **안 될 때 무엇이 보이나요**: 미리보기가 안 바뀜, 다운로드 파일이 미리보기와 다름, 잘못된 파일을 넣었는데 기존 작업이 사라짐

## 8. AI와 내 판단 3줄

- **AI에게 맡긴 일**: 캔버스 렌더링(이미지 커버 맞춤, 텍스트 줄바꿈/글자단위 폴백), 파일 형식 검증과 거부 처리, localStorage 템플릿 CRUD, JSON 내보내기/가져오기 검증 로직 작성.
- **내가 직접 판단한 일**: 화면비 3개·극단 입력 12건 직접 테스트, 템플릿 CRUD·JSON 가져오기 시나리오 직접 실행, 완성 이미지 3개 제작(저작권 문제 없는 소재만 사용), 최종 배포·검증.
- **AI 제안을 따르지 않은 일**: [있으면 적고, 없으면 "없음"]
